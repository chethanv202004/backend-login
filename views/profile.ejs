<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Profile</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #1a1a1a;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            color: #fff;
        }

        h2 {
            color: #ff5555;
            border-bottom: 2px solid #ff5555;
            padding-bottom: 5px;
        }

        .profile-card {
            background-color: #2c2c2c;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            padding: 20px;
            width: 300px;
            margin-top: 20px;
            text-align: left;
        }

        p {
            margin: 10px 0;
            color: #b3b3b3;
        }

        hr {
            border: 0;
            height: 1px;
            background: #555;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="profile-card">
        <h2>Your Profile</h2>
        <hr>
        <p>First Name: <%= your_firstname || 'N/A' %></p>
        <p>Last Name: <%= your_lastname || 'N/A' %></p>
        <p>Date of Birth: <%= your_dob || 'N/A' %></p>
        <p>Place: <%= your_place || 'N/A' %></p>
        <p>Pincode: <%= your_pincode || 'N/A' %></p>
        <p>Education Qualification: <%= your_educationqualification || 'N/A' %></p>
    </div>
</body>
</html>
